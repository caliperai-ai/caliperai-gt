# Branding for the default CaliperGT build.
# Selected at Docker build time via: docker compose build frontend  (BRAND defaults to "caliper")
VITE_BRAND_NAME=CaliperGT
VITE_BRAND_COMPANY=Caliper AI
# Leave empty to show the logo.svg image. Set to "none" for text-only brands.
VITE_BRAND_LOGO=
