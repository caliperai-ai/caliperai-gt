# Annotation Platform — Backend

FastAPI backend for the Sensor Fusion Annotation Platform. See the repository
root [README](../README.md) for setup, configuration, and deployment.

```bash
uvicorn app.main:app --reload --port 8000   # dev
```
